# Můj první web

Jednoduchá statická stránka vytvořená jako start pro vlastní web.

## Jak spustit

Protože jde o čisté HTML/CSS/JS, stačí otevřít `index.html` v prohlížeči.

Pro lokální server můžeš použít například:

```bash
python3 -m http.server 8000
```

Pak otevři `http://localhost:8000`.
